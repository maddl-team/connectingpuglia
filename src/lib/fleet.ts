/** Shared fleet copy — single Audi A4 sedan. */
export const FLEET_VEHICLE = 'Audi A4'
export const FLEET_VEHICLE_SHORT = 'Audi A4 sedan'
export const FLEET_CAPACITY = 'up to 3 passengers'
export const FLEET_LUGGAGE = '3 large suitcases'
export const FLEET_META_LUXURY = 'luxury Audi A4 sedan'

export const fleetVehicleFaq =
  `${FLEET_VEHICLE_SHORT} (${FLEET_CAPACITY}). Air-conditioned, WiFi on request, child seats available.`

export const fleetFamilyFaq =
  `The ${FLEET_VEHICLE} is ideal for couples and small groups of ${FLEET_CAPACITY}. Child seats available on request. For larger parties, contact us and we will arrange additional vehicles.`

export const fleetGroupFaq =
  `For groups larger than 3 passengers, contact us with your guest numbers and arrival details — we can coordinate additional ${FLEET_VEHICLE} vehicles.`

export const fleetMetaDescription =
  `ConnectinPuglia operates a premium ${FLEET_VEHICLE_SHORT}. Fully NCC licensed, insured, and immaculately maintained.`

export const fleetHomeDescription =
  `${FLEET_VEHICLE_SHORT}, immaculately maintained.`

export const fleetTourFaq =
  `Our tours are 100% private — for your group only. The ${FLEET_VEHICLE} accommodates ${FLEET_CAPACITY}. Price is per vehicle, not per person.`

export const fleetPageTitle = `Our Fleet — ${FLEET_VEHICLE} | ConnectinPuglia`

export const fleetImageAlt = `${FLEET_VEHICLE_SHORT} — ConnectinPuglia private transfer vehicle`
